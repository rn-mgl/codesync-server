services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        - NODE_ENV=development
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev
    ports:
      - "8000:8000"

  database:
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: true
