services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        - NODE_ENV=development
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/node_modules
    command: npm run dev

  database:
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: true
