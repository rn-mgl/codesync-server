services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.prod
      args:
        - NODE_ENV=production
    env_file:
      - .env
    command: npm run start
  database:
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORDS: true
